package foam.dao.index;

/** Convenience wrapper for indexes that want to create a closure function
    for each plan instance */
public class CustomPlan /* extends AbstractPlan */ {

    // DO we really need it?
}
